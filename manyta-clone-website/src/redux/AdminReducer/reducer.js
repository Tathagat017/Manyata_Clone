const initialstate = {
  isrequest: false,
  isauth: false,
  token: "",
  iserror: false,
};
export const reducer = (state = initialstate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
